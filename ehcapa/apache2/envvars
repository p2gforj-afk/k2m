# envvars - default environment variables for apache2ctl

# this won't be correct after changing uid
unset HOME

# for supporting multiple apache2 instances
if [ "${APACHE_CONFDIR##/etc/apache2-}" != "${APACHE_CONFDIR}" ] ; then
	SUFFIX="-${APACHE_CONFDIR##/etc/apache2-}"
else
	SUFFIX=
fi

# Since there is no sane way to get the parsed apache2 config in scripts, some
# settings are defined via environment variables and then used in apache2ctl,
# /etc/init.d/apache2, /etc/logrotate.d/apache2, etc.

# CM mars 2022
# on met les valeurs de l'utilisateur courant

export APACHE_RUN_USER=`id -un`
export APACHE_RUN_GROUP=`id -gn`

# temporary state file location. This might be changed to /run in Wheezy+1

# CM mars 2022
# modification des chemins

export APACHE_PID_FILE=/users/elo/rchaigne/ahhhhhhhhhh/ehcapa/apache2/var/run/apache2.pid
export APACHE_RUN_DIR=/users/elo/rchaigne/ahhhhhhhhhh/ehcapa/apache2/var/run
export APACHE_LOCK_DIR=/users/elo/rchaigne/ahhhhhhhhhh/ehcapa/apache2/var/lock
# Only /var/log/apache2 is handled by /etc/logrotate.d/apache2.
export APACHE_LOG_DIR=/users/elo/rchaigne/ahhhhhhhhhh/ehcapa/apache2/log

## The locale used by some modules like mod_dav
export LANG=C
## Uncomment the following line to use the system default locale instead:
#. /etc/default/locale

export LANG

## The command to get the status for 'apache2ctl status'.
## Some packages providing 'www-browser' need '--dump' instead of '-dump'.
#export APACHE_LYNX='www-browser -dump'

## If you need a higher file descriptor limit, uncomment and adjust the
## following line (default is 8192):
#APACHE_ULIMIT_MAX_FILES='ulimit -n 65536'

## If you would like to pass arguments to the web server, add them below
## to the APACHE_ARGUMENTS environment.
#export APACHE_ARGUMENTS=''

## Enable the debug mode for maintainer scripts.
## This will produce a verbose output on package installations of web server modules and web application
## installations which interact with Apache
#export APACHE2_MAINTSCRIPT_DEBUG=1
